org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx1536m

# Where photo logging goes when the user has not set a Gemini key of their own. Not a secret:
# the proxy refuses anyone without a Firebase token minted by this project.
tracker.proxyUrl=https://nutrioto-proxy.vercel.app
